{
  "client": {
    "name": "<%= @hostname %>",
    "address": "<%= @fqdn %>",
     "subscriptions": [
       "<%= @hostname %>",
       <% @subscriptions.each do |sub| %>"<%= sub %>",<% end %>
       <% @enc_classes.each do |c| %>"<%= c %>",<% end %>
       "core"
      ]
  }
}
