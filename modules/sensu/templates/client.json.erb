{
  "client": {
    "name": "<%= @hostname %>",
    "address": "<%= @fqdn %>",
     "subscriptions": [
       "<%= @hostname %>",
       <% @subscriptions.each do |sub| %>"<%= sub %>",<% end %>
       "core"
      ]
  }
}
